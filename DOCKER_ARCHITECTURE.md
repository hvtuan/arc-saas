# 🏗️ ARC SaaS Docker Architecture

Visual architecture và flow diagrams cho Docker setup.

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         External Access Layer                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  http://localhost:3005  →  Tenant Management API                     │
│  http://localhost:3002  →  Subscription API                          │
│  http://localhost:5050  →  pgAdmin                                   │
│  http://localhost:8081  →  Redis Commander                           │
│                                                                       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
┌───────────────────────────────┴─────────────────────────────────────┐
│                      Docker Network: arc-saas-network               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              Application Services Layer                      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │                                                              │   │
│  │  ┌──────────────────────────┐  ┌──────────────────────────┐│   │
│  │  │ Tenant Management Service│  │ Subscription Service     ││   │
│  │  ├──────────────────────────┤  ├──────────────────────────┤│   │
│  │  │ LoopBack 4 Application   │  │ LoopBack 4 Application   ││   │
│  │  │ - REST API               │  │ - REST API               ││   │
│  │  │ - Controllers            │  │ - Controllers            ││   │
│  │  │ - Services               │  │ - Services               ││   │
│  │  │ - Repositories           │  │ - Repositories           ││   │
│  │  │ - Models                 │  │ - Models                 ││   │
│  │  │                          │  │                          ││   │
│  │  │ Port: 3005               │  │ Port: 3002               ││   │
│  │  │ Health: /ping            │  │ Health: /ping            ││   │
│  │  │ Swagger: /explorer       │  │ Swagger: /explorer       ││   │
│  │  └────────────┬─────────────┘  └────────────┬─────────────┘│   │
│  │               │                               │              │   │
│  └───────────────┼───────────────────────────────┼──────────────┘   │
│                  │                               │                  │
│  ┌───────────────┴───────────────────────────────┴──────────────┐   │
│  │                  Data Layer                                   │   │
│  ├───────────────────────────────────────────────────────────────┤   │
│  │                                                               │   │
│  │  ┌────────────────────┐  ┌────────────────────┐             │   │
│  │  │ PostgreSQL         │  │ PostgreSQL         │             │   │
│  │  │ (Tenant Mgmt)      │  │ (Subscription)     │             │   │
│  │  ├────────────────────┤  ├────────────────────┤             │   │
│  │  │ Database:          │  │ Database:          │             │   │
│  │  │ tenant_mgmt_db     │  │ subscription_db    │             │   │
│  │  │                    │  │                    │             │   │
│  │  │ Tables:            │  │ Tables:            │             │   │
│  │  │ - tenants          │  │ - plans            │             │   │
│  │  │ - leads            │  │ - subscriptions    │             │   │
│  │  │ - contacts         │  │ - billing_cycles   │             │   │
│  │  │ - invoices         │  │ - currencies       │             │   │
│  │  │ - resources        │  │ - plan_sizes       │             │   │
│  │  │ - addresses        │  │ - resources        │             │   │
│  │  │                    │  │                    │             │   │
│  │  │ Port: 5432         │  │ Port: 5432         │             │   │
│  │  │ Exposed: 5432      │  │ Exposed: 5433      │             │   │
│  │  └────────────────────┘  └────────────────────┘             │   │
│  │                                                               │   │
│  │  ┌────────────────────────────────────────────┐             │   │
│  │  │ Redis                                       │             │   │
│  │  ├────────────────────────────────────────────┤             │   │
│  │  │ Purpose:                                    │             │   │
│  │  │ - Caching                                   │             │   │
│  │  │ - Session storage                           │             │   │
│  │  │ - Webhook secrets (temp)                    │             │   │
│  │  │ - BullMQ message queue                      │             │   │
│  │  │                                             │             │   │
│  │  │ Databases:                                  │             │   │
│  │  │ - DB 0: Tenant Management cache            │             │   │
│  │  │ - DB 1: Subscription cache                 │             │   │
│  │  │                                             │             │   │
│  │  │ Port: 6379                                  │             │   │
│  │  └────────────────────────────────────────────┘             │   │
│  │                                                               │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │               Management Tools Layer                         │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │                                                              │   │
│  │  ┌──────────────────┐        ┌──────────────────┐          │   │
│  │  │ pgAdmin          │        │ Redis Commander  │          │   │
│  │  ├──────────────────┤        ├──────────────────┤          │   │
│  │  │ Web UI for       │        │ Web UI for       │          │   │
│  │  │ PostgreSQL DBs   │        │ Redis management │          │   │
│  │  │                  │        │                  │          │   │
│  │  │ Port: 5050       │        │ Port: 8081       │          │   │
│  │  └──────────────────┘        └──────────────────┘          │   │
│  │                                                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│                         Persistent Storage                            │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  Docker Volumes:                                                      │
│  - arc-saas-postgres-tenant-data                                      │
│  - arc-saas-postgres-subscription-data                                │
│  - arc-saas-redis-data                                                │
│  - arc-saas-pgadmin-data                                              │
│                                                                        │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow: Create Tenant

```
┌──────────┐
│  Client  │
└────┬─────┘
     │ POST /tenants
     │ Authorization: Bearer <JWT>
     ▼
┌─────────────────────────────────┐
│ Tenant Management Service       │
│ (Port 3005)                     │
├─────────────────────────────────┤
│ 1. Authentication               │
│    - Verify JWT token           │
│    - Extract user context       │
│                                 │
│ 2. Authorization                │
│    - Check permissions          │
│    - Validate request           │
│                                 │
│ 3. Controller Layer             │
│    TenantController.create()    │
│         │                       │
│         ▼                       │
│ 4. Service Layer                │
│    ProvisioningService          │
│    - Validate tenant data       │
│    - Create tenant record       │
│    - Generate tenant key        │
│         │                       │
│         ▼                       │
│ 5. Repository Layer             │
│    TenantRepository.create()    │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ PostgreSQL (Tenant DB)          │
│ Port: 5432                      │
├─────────────────────────────────┤
│ INSERT INTO main.tenants        │
│ VALUES (...)                    │
│         │                       │
│         └─ COMMIT               │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Event Publishing                │
├─────────────────────────────────┤
│ EventConnector.publish()        │
│ Event: TENANT_PROVISIONING      │
│         │                       │
│         ▼                       │
│ Message Bus                     │
│ (EventBridge/SQS/BullMQ)        │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Cache Update                    │
├─────────────────────────────────┤
│ Redis DB 0                      │
│ SET tenant:<id> <data>          │
│ EXPIRE tenant:<id> 3600         │
└─────────────────────────────────┘
         │
         │ Response
         ▼
┌──────────┐
│  Client  │
│ 201 Created                     │
│ { id, name, key, status }       │
└──────────┘
```

---

## 🔄 Request Flow: Create Subscription

```
┌──────────┐
│  Client  │
└────┬─────┘
     │ POST /subscriptions
     │ Authorization: Bearer <JWT>
     ▼
┌─────────────────────────────────┐
│ Subscription Service            │
│ (Port 3002)                     │
├─────────────────────────────────┤
│ 1. Authenticate & Authorize     │
│    - Verify JWT                 │
│    - Check permissions          │
│                                 │
│ 2. Controller                   │
│    SubscriptionController       │
│         │                       │
│         ▼                       │
│ 3. Service Layer                │
│    SubscriptionService          │
│    - Validate plan exists       │
│    - Check tenant quota         │
│    - Calculate billing          │
│         │                       │
│         ▼                       │
│ 4. Repository                   │
│    SubscriptionRepository       │
└────────┬────────────────────────┘
         │
         ├─────┐
         │     │
         ▼     ▼
┌────────────┐ ┌────────────────┐
│ PostgreSQL │ │ Redis Cache    │
│ Subscription│ │ DB 1           │
│ Port: 5433 │ │                │
├────────────┤ ├────────────────┤
│ - plans    │ │ Cache plan     │
│ - subs     │ │ details        │
│ - billing  │ │                │
└────────────┘ └────────────────┘
         │
         │ Billing Event
         ▼
┌─────────────────────────────────┐
│ Billing Integration             │
│ (loopback4-billing)             │
│ - Generate invoice              │
│ - Calculate price               │
└─────────────────────────────────┘
```

---

## 📦 Container Dependencies

```
Start Order:
    1. Networks Created
       └─ arc-saas-network

    2. Volumes Created
       ├─ postgres-tenant-data
       ├─ postgres-subscription-data
       ├─ redis-data
       └─ pgadmin-data

    3. Infrastructure Services
       ├─ postgres-tenant-management (healthcheck)
       ├─ postgres-subscription (healthcheck)
       └─ redis (healthcheck)

    4. Application Services
       ├─ tenant-management-service
       │  └─ depends_on:
       │     ├─ postgres-tenant-management (healthy)
       │     └─ redis (healthy)
       │
       └─ subscription-service
          └─ depends_on:
             ├─ postgres-subscription (healthy)
             └─ redis (healthy)

    5. Management Tools
       ├─ pgadmin
       │  └─ depends_on:
       │     ├─ postgres-tenant-management
       │     └─ postgres-subscription
       │
       └─ redis-commander
          └─ depends_on:
             └─ redis

Stop Order (reverse):
    1. Management Tools
    2. Application Services
    3. Infrastructure Services
    4. Volumes (if -v flag)
    5. Networks
```

---

## 🌐 Network Communication

```
┌─────────────────────────────────────────────────────┐
│         arc-saas-network (bridge)                   │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Tenant Management ─────► PostgreSQL (tenant)       │
│       :3005               postgres-tenant-management│
│         │                      :5432                 │
│         │                                            │
│         └────────────────► Redis                    │
│                                :6379                 │
│                                  ▲                   │
│                                  │                   │
│  Subscription ──────────────────┤                   │
│       :3002              PostgreSQL (subscription)  │
│         │                postgres-subscription      │
│         │                      :5432                 │
│         │                                            │
│         └────────────────► Redis                    │
│                                :6379                 │
│                                                      │
│  pgAdmin ───────────────► PostgreSQL (both)         │
│    :5050                                             │
│                                                      │
│  Redis Commander ───────► Redis                     │
│    :8081                     :6379                  │
│                                                      │
│  DNS Resolution:                                     │
│  - postgres-tenant-management → 172.18.0.2          │
│  - postgres-subscription → 172.18.0.3               │
│  - redis → 172.18.0.4                               │
│  - tenant-management-service → 172.18.0.5           │
│  - subscription-service → 172.18.0.6                │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────┐
│                 Security Layers                      │
├─────────────────────────────────────────────────────┤
│                                                      │
│  1. Network Isolation                               │
│     └─ All services in private bridge network       │
│        Only exposed ports accessible from host      │
│                                                      │
│  2. Authentication                                   │
│     ├─ JWT Bearer tokens                            │
│     ├─ Token verification                           │
│     └─ Lead tokens for onboarding                   │
│                                                      │
│  3. Authorization                                    │
│     ├─ Permission-based access control              │
│     ├─ Role-based permissions                       │
│     └─ Resource-level authorization                 │
│                                                      │
│  4. Database Security                               │
│     ├─ Password authentication                      │
│     ├─ SSL/TLS support (configurable)               │
│     ├─ Schema isolation (main schema)               │
│     └─ Connection pooling                           │
│                                                      │
│  5. Secrets Management                              │
│     ├─ Environment variables                        │
│     ├─ Docker secrets (production)                  │
│     └─ Webhook secrets in Redis (expiring)          │
│                                                      │
│  6. API Security                                     │
│     ├─ Request validation                           │
│     ├─ Rate limiting (loopback4-ratelimiter)        │
│     ├─ CORS configuration                           │
│     └─ Webhook signature verification               │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 💾 Data Flow & Persistence

```
┌─────────────────────────────────────────────────────┐
│              Write Operations                        │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Client Request                                      │
│       ▼                                              │
│  API Service (validate)                             │
│       ▼                                              │
│  Repository (ORM)                                    │
│       ▼                                              │
│  PostgreSQL (transactional)                          │
│       │                                              │
│       ├─ Write to WAL                               │
│       ├─ Commit to disk                             │
│       └─ Persist to volume                          │
│              │                                       │
│              ▼                                       │
│  Docker Volume (host filesystem)                    │
│  /var/lib/docker/volumes/                           │
│    arc-saas-postgres-tenant-data/_data              │
│                                                      │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│              Read Operations                         │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Client Request                                      │
│       ▼                                              │
│  API Service                                         │
│       │                                              │
│       ├─ Check Redis cache (fast)                   │
│       │      │                                       │
│       │      ├─ HIT: Return cached data             │
│       │      │                                       │
│       │      └─ MISS: Query database                │
│       │             │                                │
│       └─────────────┴─► PostgreSQL                  │
│                              │                       │
│                              ├─ Read from disk       │
│                              └─ Update cache         │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 Scaling Strategy

### Horizontal Scaling

```
┌─────────────────────────────────────────────────────┐
│               Load Balancer (Future)                 │
│                 nginx / ALB                          │
└────────┬────────────────────────┬────────────────────┘
         │                        │
    ┌────▼────┐              ┌────▼────┐
    │ Service │              │ Service │
    │ Node 1  │              │ Node 2  │
    │ :3005   │              │ :3006   │
    └────┬────┘              └────┬────┘
         │                        │
         └────────┬───────────────┘
                  │
         ┌────────▼────────┐
         │   PostgreSQL    │
         │   (Primary)     │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │   PostgreSQL    │
         │   (Replica)     │
         └─────────────────┘
```

### Vertical Scaling

```yaml
# docker-compose.yml
services:
  tenant-management-service:
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G
```

---

## 🔍 Monitoring Architecture

```
┌─────────────────────────────────────────────────────┐
│              Observability Stack                     │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Application Metrics                                │
│  ├─ Swagger Stats (built-in)                       │
│  │  └─ /stats endpoint                             │
│  │                                                  │
│  └─ OpenTelemetry                                  │
│     ├─ Jaeger exporter                             │
│     └─ Distributed tracing                         │
│                                                      │
│  Health Checks                                      │
│  ├─ /ping endpoints                                │
│  ├─ Docker healthcheck                             │
│  └─ pg_isready / redis-cli ping                    │
│                                                      │
│  Logs                                               │
│  ├─ stdout/stderr (JSON)                           │
│  ├─ docker-compose logs                            │
│  └─ Centralized logging (future)                   │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 📝 Configuration Management

```
┌─────────────────────────────────────────────────────┐
│           Configuration Sources                      │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Priority (highest to lowest):                      │
│                                                      │
│  1. Environment Variables                           │
│     └─ docker-compose.yml → environment:            │
│                                                      │
│  2. .env files                                      │
│     ├─ .env.docker (custom)                        │
│     └─ .env.defaults (fallback)                    │
│                                                      │
│  3. Application Defaults                            │
│     └─ Hardcoded in datasource configs             │
│                                                      │
│  4. Runtime Configuration                           │
│     └─ LoopBack binding providers                  │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🎯 Summary

This architecture provides:

✅ **Isolation**: Each service has its own database
✅ **Scalability**: Stateless services, scalable data layer
✅ **Resilience**: Health checks, restart policies
✅ **Security**: Network isolation, authentication, authorization
✅ **Observability**: Logging, metrics, tracing
✅ **Maintainability**: Clear separation of concerns
✅ **Developer Experience**: Hot reload, debugging, management tools

---

## 📚 Related Documentation

- [Docker Compose Configuration](./docker-compose.yml)
- [Setup Guide](./DOCKER_SETUP.md)
- [Quick Start](./QUICK_START.md)
- [Files Summary](./DOCKER_FILES_SUMMARY.md)
